console.error(
  'This command is deprecated because it has been moved to the the Xata CLI https://github.com/xataio/client-ts/tree/main/cli'
);
console.error(
  'Please, install it, configure your project with `xata init` and run `xata codegen` to generate your XataClient for every schema change.'
);
process.exit(1);

export {};
