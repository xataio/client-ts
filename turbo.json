{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "add-version": {},
    "tsc": {
      "dependsOn": ["add-version", "^tsc"],
      "inputs": ["src/**"]
    },
    "build": {
      "dependsOn": ["tsc", "^build"],
      "outputs": ["dist/**"]
    }
  }
}
