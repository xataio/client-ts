{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "add-version": {},
    "tsc": {
      "dependsOn": ["add-version"],
      "inputs": ["src/**"]
    },
    "build": {
      "dependsOn": ["tsc", "^build"],
      "outputs": ["dist/**"]
    }
  }
}
